<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Panel</title>
    <style>
        *
        {
            margin: 0;
            padding: 0;
        }
        .main
        {
            width: 100%;
            height: 100vh;
            background: #555;
            
        }
        .login-form
        {
            width: 20%;
            height: 60vh;
            margin-top: 7%;
            margin-left: 40%;
            position: absolute;
            background: #f6feff;
            border-radius: 10px;
            box-shadow: 0 0 10px #000,
                        0 0 50px #000,
                        0 0 100px #000;
        }
        .input
        {
            position: absolute;
            margin-top: 70px;
            margin-left: 20px;
            opacity: 1 !important;
        }
        .input-text
        {
            margin-left: 20px;
            margin-top: 20px;
            padding: 10px;
            border: none;
            background: none;
            opacity: 1 !important;
            border-radius: 5px;
            border-bottom: 1px solid #000 !important;
        }
        .input-text::placeholder
        {
            color: #000;
            opacity: 1 !important;
        }
        .input-btn
        {
            margin-top: 30px;
            padding: 10px;
            margin-left: 40px;
            background: none;
            border: none;
            border-radius: 5px;
            border: 1px solid #000 !important;
        }
        .title
        {
            margin-left: 70px;
            margin-top: 10px;
            position: absolute;    
        }
    </style>
</head>
<body>
       <section class="main">
      
        <form action="/login" class="login-form", method="POST">
            <%include ("./partials/messages")%>
            <h1 class="title">Login Panel</h1>
            <div class="input">
                <input class="input-text" type="text" name="email" placeholder="User Name" />
                <input class="input-text" type="password" name="password" placeholder="password" />
                <button class="input-btn">Login</button>    
            </div>
        </form>
    </section>
</body>
</html>